name: Schedule Test

on:
  schedule:
    - cron: "*/5 * * * *"

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print current UTC time
        run: |
          echo "Scheduled workflow fired"
          date -u